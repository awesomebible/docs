[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "awesomebible-docs"
authors = [{name = "awesomeBible", email = "hi@awesomeBible.de"}]
dynamic = ["version", "description"]
